'use strict';

// Create a program that prints all the even numbers between 0 and 500

let numberofexec: number = 500;

for (let index = 0; index <numberofexec ; index++) 
    
    if (index % 2 === 0) {
    console.log (index);   
}